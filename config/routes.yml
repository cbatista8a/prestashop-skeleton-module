example:
  path: cdskeletron/example
  methods: [GET]
  defaults:
    _controller: 'CubaDevOps\controllers\admin\Example::example'
    _disable_module_prefix: true

#    Todo attach csrf token automatically to route url for prevent error on BO
